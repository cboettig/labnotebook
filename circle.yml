machine:
  services:
    - docker

test:
  override:
    - docker run -ti -e GH_TOKEN=$GH_TOKEN \
      -e TWIT_KEY=$TWIT_KEY -e TWIT_SECRET=$TWIT_SECRET \
      -e TWIT_TOK=$TWIT_TOK -e TWIT_SECRET_KEY=$TWIT_SECRET_KEY \
      cboettig/labnotebook Rscript _build/deploy.R 

